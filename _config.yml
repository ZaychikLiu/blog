# Hexo site configuration
# Docs: https://hexo.io/docs/configuration

# Site
title: Feiyang Liu's Blog
subtitle:
description: Notes on research, engineering, and everyday learning.
author: Feiyang Liu
language: en
timezone: Asia/Shanghai

# URL
# Default target: https://zaychikliu.github.io/blog/
# If you use a custom domain root, set url to that domain and root to /
url: https://zaychikliu.github.io/blog
root: /blog/
permalink: :year/:title/
permalink_defaults:

# Directory
source_dir: source
public_dir: public
tag_dir: tags
archive_dir: archives
category_dir: categories
code_dir: downloads/code
i18n_dir: :lang
skip_render:

# Writing
new_post_name: :title.md
default_layout: post
titlecase: false
external_link:
  enable: true
  field: post
  exclude:
filename_case: 0
render_drafts: false
post_asset_folder: true
relative_link: false
future: true

# Markdown and code highlighting
highlight:
  enable: true
  line_number: true
  auto_detect: false
  tab_replace:
  wrap: true
  hljs: false
prismjs:
  enable: false

# Category & tag
default_category: Blog
category_map:
tag_map:

# Date / time format
date_format: DD/MMM/YYYY
time_format: HH:mm:ss
updated_option: empty

# Pagination
per_page: 10
pagination_dir: page

index_generator:
  per_page: 10

archive_generator:
  per_page: 20
  yearly: true
  monthly: true

category_generator:
  per_page: 10

tag_generator:
  per_page: 10

# Theme
theme: icarus

# RSS, exposed at /blog/atom.xml
feed:
  type: atom
  path: atom.xml
  limit: 20
  hub:
  content: true
  content_limit:
  content_limit_delim: <!-- more -->
  order_by: -date

# Optional GitHub emoji support
githubEmojis:
  className: not-gallery-item

# SEO
sitemap:
  path: sitemap.xml

robotstxt:
  useragent: "*"
  allow:
    - /
  sitemap: https://zaychikliu.github.io/blog/sitemap.xml

# Optional advanced features injected by scripts/advanced-features.js.
# Keep empty values until you create the corresponding third-party service.
advanced:
  asset_version: 20260710-collaboration-badges-v2
  category_links:
    - name: Blog
      path: /categories/Blog/
    - name: Research
      path: /categories/Research/
  bilingual_titles:
    - path: /2026/beyond-vision/
      zh: Vision之外
      en: Beyond Vision
  reading_progress: true
  heading_anchors: true
  share:
    enabled: true
    xiaohongshu_profile: https://www.xiaohongshu.com/user/profile/62a32fde000000001b02936e
    xiaohongshu_text: "{title}\n{url}"
    platforms:
      - x
      - email
      - xiaohongshu
      - copy
  popular_posts:
    enabled: false
    title: Popular Posts
    posts: []
  toc:
    enabled: true
    title: Contents
    min_headings: 1
  comments:
    enabled: true
    # provider can be "giscus" or "utterances".
    provider: utterances
    # Giscus needs repo, repo_id, category, and category_id.
    # Get them from https://giscus.app after enabling GitHub Discussions.
    repo: ZaychikLiu/blog
    repo_id:
    category: General
    category_id:
    mapping: pathname
    reactions_enabled: "1"
    emit_metadata: "0"
    input_position: bottom
    theme: light
    lang: en
    # Utterances only needs repo.
    utterances_repo: ZaychikLiu/blog
    utterances_issue_term: pathname
    utterances_theme: github-light
  analytics:
    google_analytics:
      measurement_id:
    plausible:
      domain:
      script_url: https://plausible.io/js/script.js
  edit_link:
    enabled: false
    label: Edit this page
    repository: https://github.com/ZaychikLiu/blog
    branch: main

# Optional deployer. GitHub Actions is preferred, but this works after gh/git auth.
deploy:
  type: git
  repository: git@github.com:ZaychikLiu/blog.git
  branch: gh-pages

marked:
  gfm: true
  breaks: false
  mangle: false
  headerIds: true
